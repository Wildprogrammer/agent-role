# 贡献指南

## 工作流能力归属

单一工作流只拥有本领域可独立复用的能力；复合流程只编排其他工作流，并在自身文档中保存组合顺序、跨步骤状态和专属约束。不得把复合流程的调用方约束反向写成被调用工作流的通用能力，也不得复制已有实现形成第二权威。

## 变更要求

1. 修改前阅读根 `SKILL.md` 和目标工作流 `SKILL.md`。
2. 新增或修改行为时先更新可观察测试，再实施最小改动。
3. 保留用户文件、目标系统 RBAC 和现有工作流能力边界。
4. 文档和示例不得包含真实凭据、个人绝对路径或私有环境地址。
5. 提交前运行 `python -m pytest -q` 和 `workflow-hub validate "<absolute-hub-root>"`。

## 提交

提交应范围单一、说明清楚，不包含缓存、虚拟环境、运行输出、真实配置或无关格式化。
