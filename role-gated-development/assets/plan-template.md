# 实施计划

仅在计划必须跨会话保存或交给其他执行者时使用此文件。普通计划优先使用当前规划工具或内联步骤。

默认只保留以下核心章节，不预设状态变化或独立审查章节。实际触发时才按需加入最小记录；已有 `STATUS.md`、`task.md` 或 review 文件负责该信息时，不在计划中重复。

## 目标


## 成功标准

-

## 范围与非目标

范围：

非目标：

## 实施步骤

1.

## 风险与权限边界

-
